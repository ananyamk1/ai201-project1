sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Ingestion + cleaning (Milestone 3)
requests==2.34.2
beautifulsoup4>=4.13
lxml>=5.0
# Browser capture for bot-walled sources (Reddit/METRO/reviews).
# After install run once: playwright install chromium
playwright>=1.40
playwright-stealth>=2.0

# Generation + interface (Milestone 5)
# gradio 5.x keeps huggingface-hub < 1.0, which transformers (used by the
# embedder) requires; gradio 6 forces hub >= 1.2 and breaks transformers.
gradio>=5,<6
huggingface-hub<1.0

# Stretch feature: hybrid (BM25 + vector) retrieval
rank-bm25>=0.2.2

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
